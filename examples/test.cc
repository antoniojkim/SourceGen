#include <iostream>
#include <vector>

using namespace std;

vector<int> test_vector = {
    // begin autogen test_vector
    // end autogen
};

int main() {
    for (auto& e : test_vector) { cout << e << endl; }
}
